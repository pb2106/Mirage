[package]
name = "mirage-diversity"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
uuid = { version = "1.3", features = ["v4"] }
flate2 = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
mirage-protocol = { path = "../mirage-protocol" }
