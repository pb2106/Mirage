# Phase 0: Scaffolding

This phase initializes the `mirage` workspace with the foundational structure:
- Cargo workspace with member crates for all architectural components.
- CI skeleton using GitHub Actions.
- MIT/Apache-2.0 dual license.
- README with the Project Charter.
- Basic CLI stub that supports `mirage --version`.

All tests pass (a basic `cargo test` runs without issue, though tests are empty for now).
Next up is Phase 1 (Audit-only MVP).
